package testBench.common;


actor CheckU8(String portName)
	uint(size=8) RealValue,
	uint(size=8) ValueGenerated
		==>
		:

	action RealValue:[r], ValueGenerated:[v] ==> 
	do
		if(v!=r) then
			println("Port "+portName+" : "+v+" instead of "+r);
		end
	end
end