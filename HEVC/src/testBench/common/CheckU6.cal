package testBench.common;


actor CheckU6(String portName)
	uint(size=6) RealValue,
	uint(size=6) ValueGenerated
		==>
		:

	action RealValue:[r], ValueGenerated:[v] ==> 
	do
		if(v!=r) then
			println("Port "+portName+" : "+v+" instead of "+r);
		end
	end
end