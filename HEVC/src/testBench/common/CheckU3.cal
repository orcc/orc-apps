package testBench.common;


actor CheckU3(String portName)
	uint(size=3) RealValue,
	uint(size=3) ValueGenerated
		==>
		:

	action RealValue:[r], ValueGenerated:[v] ==> 
	do
		if(v!=r) then
			println("Port "+portName+" : "+v+" instead of "+r);
		end
	end
end