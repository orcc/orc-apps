package org.ietr.mpegh.part2.common;
import  std.video.Display.*;

actor FpsCompute () bool DoneY, bool DoneU, bool DoneV ==> :

	initialize ==>
	do
		fpsPrintInit();
	end
	
	action DoneY:[y], DoneU:[u], DoneV:[v] ==>
	do
		fpsPrintNewPicDecoded();
	end
end