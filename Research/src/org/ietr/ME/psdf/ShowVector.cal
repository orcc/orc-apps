package org.ietr.ME.psdf;

import std.util.Math.* ;

actor ShowVector () uint(size=8) MB, uint(size=8) MV ==>  :
		
	action MB:[x,y], MV:[vx, vy]==>
	do
	println(" Best vector for MB: "+ x + ", "+ y +" is vector : ["+ vx +", "+ vy +".");
	end
	
end
