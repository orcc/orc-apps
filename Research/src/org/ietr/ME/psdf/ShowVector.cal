package org.ietr.ME.psdf;

import std.util.Math.* ;

actor ShowVector () uint(size=8) MB, int(size=32) MV ==>  :
		
	action MB:[id], MV:[vx, vy]==>
	do
	println(" Best vector for MB: "+ id + " is vector : ["+ vx +", "+ vy +".");
	end
	
end
