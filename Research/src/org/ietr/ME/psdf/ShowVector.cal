package org.ietr.ME.psdf;

import std.util.Math.* ;

actor ShowVector () int(size=32) MB, int(size=32) x, int(size=32) y ==>  :
		
	action MB:[id], x:[vx], y:[vy]==>
	do
	println(" Best vector for id: "+ id +" x:" + vx +" y: "+ vy);
	end
	
end
