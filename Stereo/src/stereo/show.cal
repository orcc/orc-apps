/*
 * Author: Jinglin
 * Date: 16/01/13 
 * do nothing now
 */
package stereo;

actor show() int result ==> :

int height = 375;
int width = 450;

	// show actor do nothing, just for test
	 test: action result:[input] repeat width*height ==>
	 var 
		 List (type: int, size = width*height) output
	 do
		 foreach int i in 0 .. (width*height-1)
		 do
		 	output [i] := input[i];
		 end 
	 end		
end