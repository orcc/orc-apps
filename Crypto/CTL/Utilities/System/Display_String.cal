/**
 * This FU is used to display a sequence of string (text) tokens.
 * 
 * FU ID: Display_String
 * One input port:
 *   1) String IP: the sequence of string tokens.
 * 
 * Developers: Junaid Jameel Ahmad @ Uni-Konstanz 2012
 */

package Utilities.System;

actor Display_String () String IP ==> :

	action IP:[x] ==>
	do
		println(x);
	end
end
