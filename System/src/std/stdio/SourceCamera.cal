package std.stdio;

import  std.header.Parameter.*;

unit SourceCamera:

	uint(size=32) IM_WIDTH = 640;
	uint(size=32) IM_HEIGHT = 480;

	@native procedure source_camera_init(int width, int height)
	end

	@native procedure readCameraFrame(uint(size=8) rArr[IM_WIDTH*IM_HEIGHT], uint(size=8) gArr[IM_WIDTH*IM_HEIGHT], uint(size=8) bArr[IM_WIDTH*IM_HEIGHT] )
	end

	
end