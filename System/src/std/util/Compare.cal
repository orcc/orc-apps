package std.util;

unit Compare:
	uint(size=32) NB_TOKEN_TO_COMPARE = 4096;
	
	@native procedure compare_init()
	end

	@native procedure compare_close()
	end

	@native procedure compare_readByte(uint(size=8) byte)
	end

	@native procedure compare_NBytes(uint(size=8) outTable[NB_TOKEN_TO_COMPARE], uint nbTokenToRead)
	end

	@native procedure compare_NBytesNext(uint(size=8) outTable[NB_TOKEN_TO_COMPARE], uint nbTokenToRead, uint(size = 8) display)
	end
end